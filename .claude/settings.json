{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-level Claude Code settings for automation-registry. Tracked. Local-only overrides go in .claude/settings.local.json. SessionStart hook = health probe (skeleton AR-S2).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/health_check.py",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
